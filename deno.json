{
  "name": "@nullstyle/usession",
  "version": "0.2.0",
  "license": "MIT",
  "description": "Rails-like sealed (encrypted) cookie sessions. Framework and runtime agnostic — needs only Web Crypto and the Fetch Request.",
  "exports": "./mod.ts",
  "imports": {
    "tweetnacl": "npm:tweetnacl@^1.0.3",
    "@std/encoding": "jsr:@std/encoding@^1",
    "@std/assert": "jsr:@std/assert@^1"
  },
  "tasks": {
    "test": "deno test -A",
    "check": "deno check **/*.ts",
    "lint": "deno lint",
    "fmt": "deno fmt --check",
    "doc": "deno doc --lint mod.ts",
    "cov": "deno test -A --coverage=cov_profile && deno coverage cov_profile",
    "ok": "deno task fmt && deno task lint && deno task check && deno task doc && deno task test"
  },
  "publish": {
    "exclude": ["**/*_test.ts", "deno.lock", ".github", "CHANGELOG.md"]
  }
}
